'use strict';

// Public: Create a new Channel instance
//
// name - A String with the name of the channel
//
// Returns the new Channel instance
var Channel = function(name){
	this.name = name;
};

module.exports = Channel;
